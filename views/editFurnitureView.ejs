<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Edit Furniture</title>
    <link rel="stylesheet" href="/styles/add.css">
</head>
<body>
    <h1>Edit Furniture</h1>
    <div>
        <form action="/furnitures/edit/<%= editFurniture._id %>?_method=PUT" method="POST">
            <label for="furniturename">Name:</label>
            <input type="text" id="furniturename" name="furniturename" value="<%= editFurniture.furniturename %>" required>
            <br>
            <label for="price">Price:</label>
            <input type="number" id="price" name="price" step="0.01" value="<%= editFurniture.price %>" required>
            <br>
            <label for="location">Location:</label>
            <input type="text" id="location" name="location" value="<%= editFurniture.location %>" required>
            <br>
            <label for="date">Date:</label>
            <input type="date" id="date" name="date" value="<%= new Date(editFurniture.date).toISOString().split('T')[0] %>" required>
            <br>
            
            <button type="submit">Update Furniture</button>
        </form>
        <a href="/furnitures">Back To Furnitures List</a>
    </div>
</body>
</html>